[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tactus"
version = "0.1.0"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
